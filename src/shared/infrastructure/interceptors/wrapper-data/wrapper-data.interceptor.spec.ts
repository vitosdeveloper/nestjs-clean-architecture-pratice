import { DataInterceptorWrapper } from './wrapper-data.interceptor';

describe('DataInterceptorWrapper', () => {
  it('should be defined', () => {
    expect(new DataInterceptorWrapper()).toBeDefined();
  });
});
